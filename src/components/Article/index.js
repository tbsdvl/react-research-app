import React from "react";
import { Card } from "react-bootstrap";

const Article = ({ data }) => {
    console.log(data);

    return (
        <Card>
            {/* <p>{data.}</p> */}
        </Card>
    );
};

export default Article;